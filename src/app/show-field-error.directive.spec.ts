import { ShowFieldErrorDirective } from './show-field-error.directive';

describe('ShowFieldErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowFieldErrorDirective();
    expect(directive).toBeTruthy();
  });
});
